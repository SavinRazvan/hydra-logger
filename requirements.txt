# Core dependencies for hydra-logger
pydantic>=2.10.0
pyyaml>=6.0.3
aiofiles>=24.0.0
python-json-logger>=4.0.0
graypy>=2.1.0
pytz>=2025.0
toml>=0.10.2
msgpack>=1.1.1
psutil>=7.1.0
requests>=2.32.0
urllib3>=1.26.0,<3.0.0
charset-normalizer>=3.4.0

# Database handlers (optional)
psycopg2-binary>=2.9.10  # PostgreSQL
pymongo>=4.10.0          # MongoDB
redis>=6.1.0            # Redis

# Message queue handlers (optional)
pika>=1.3.2             # RabbitMQ
kafka-python>=2.2.0     # Kafka

# Cloud service handlers (optional)
boto3>=1.37.0           # AWS CloudWatch
elasticsearch>=9.0.0    # Elasticsearch

# Network handlers (optional)
websockets>=13.0.0      # WebSocket support

# System handlers (optional - Windows only)
pywin32>=306; sys_platform == 'win32'  # Windows Event Log